# PostgreSQL database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/vcfriend_db
spring.datasource.username=postgres
spring.datasource.password=Javaholics@95

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# File upload configuration
vcf.upload.dir=uploads/vcf

# Cross-origin session cookie handling (for local dev with frontend on a different port)
server.servlet.session.cookie.same-site=None
server.servlet.session.cookie.secure=false
server.address=0.0.0.0
